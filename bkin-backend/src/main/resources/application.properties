# Updated for Render + Aiven Deployment
#server.port=${PORT:8090}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://mysql-2a11dbee-srihitha-8f06.k.aivencloud.com:10291/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=avnadmin
spring.datasource.password=AVNS__2FBifXDSNx6gCIgQqO

#spring.datasource.url=jdbc:mysql://localhost:3306/bkin_db
#spring.datasource.username=root
#spring.datasource.password=aBhi@2004

# Add these to prevent the crash-loop
spring.datasource.hikari.initialization-fail-timeout=0
spring.datasource.continue-on-error=true
server.port=${PORT:8090}

# Gemini API Configuration
# Key is set via GEMINI_API_KEY environment variable on Render (never hardcode here)
google.gemini.api.key=${GEMINI_API_KEY:}
google.gemini.url=https://generativelanguage.googleapis.com/v1beta/openai/chat/completions

# ── JPA ────────────────────────────────────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# ── JWT ────────────────────────────────────────────────────────────────────────
bkin.jwt.secret=bkin-obsidian-crimson-super-secret-key-for-jwt-256bit!!
bkin.jwt.expiration-ms=86400000

# ── Upload size limits (base64 images sent as JSON) ────────────────────────────
server.tomcat.max-http-form-post-size=15MB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB

# ── Jackson ────────────────────────────────────────────────────────────────────
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false
